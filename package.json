{
  "name": "adultando",
  "version": "0.1.0",
  "private": true,
  "description": "Landing page do Adultando — captação de leads.",
  "scripts": {
    "dev": "tailwindcss -i src/input.css -o assets/css/app.css --watch",
    "build": "tailwindcss -i src/input.css -o assets/css/app.css --minify",
    "serve": "python3 -m http.server 8080",
    "build:artifact": "npm run build && python3 scripts/build-artifact.py"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/htzistrash/adultando.git"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
